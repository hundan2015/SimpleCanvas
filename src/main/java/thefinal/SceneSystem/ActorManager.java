package thefinal.SceneSystem;

import java.awt.Point;

public class ActorManager {
    static void scaleActor(ActorObject actor, Point rootPoint, Point mousePoint) {
        /* actor.scaleX = actor.lastScaleX + mousePoint.x - rootPoint.y;
        actor.scaleY = actor.lastScaleY + mousePoint.y - rootPoint.y; */
    }

    static void moveActor(ActorObject actor, Point mousePoint) {
        /* actor.posX = mousePoint.x + 1;
        actor.posY = mousePoint.y + 1; */
    }
}
